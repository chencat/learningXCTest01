name: Hello Checkout

on:
  push:
    branches: [main]

jobs:
  show-code:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the code
        uses: actions/checkout@main

      - name: Show folder contents main
        run: ls -la
